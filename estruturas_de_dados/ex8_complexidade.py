# Exercício8

def Exercício8 (dadosA = [], dadosB = []):
    sort(dadosB)
    for i in range(o, len(dadosA), 1)
        if(busca(dadosB, i) >= 0)
            return i
        
# Tsort(B) = O(B*logB)
# Tbusca(B) = O(logB)
# Vai somar o Sort + Busca: adicão
# Será O((A + B).logB)

